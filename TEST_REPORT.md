# PriceWatch 测试与交付审查

测试日期：2026-09-09。环境：Windows、Python 3.12、项目专用虚拟环境。

## 实际验证结果

- 上游原有测试和新增测试合计：27 项通过。
- 依赖检查：没有发现损坏或冲突的依赖。
- 离线 Demo：成功保存两轮、2 个商品的历史快照，并识别出 2 个变化事件。
- HTML 报告：成功生成，截图保存在 `docs/demo-report.png`。
- 测试与 Demo 使用仓库内的静态样例，不依赖真实商家网站。

## 覆盖的核心行为

- 首次快照把当前商品标记为新增。
- 后续快照识别商品新增、消失、价格变化和库存文字变化。
- 不同网站来源的历史互相隔离。
- 商品 URL 优先作为稳定标识，没有 URL 时使用规范化名称。
- 同一轮出现重复商品标识时拒绝整轮保存，避免生成不确定结果。
- HTML 中的商品名称和 URL 经过转义，降低本地报告注入风险。

## 严格结论

| 检查项 | 结论 |
|---|---|
| 能否独立运行 | 能；提供命令行入口和离线 Demo |
| 新功能是否工作 | 能；由自动测试和实际生成的 SQLite/HTML 文件验证 |
| 是否有真实代码修改 | 有；新增历史、差异、报告模块并改造 CLI |
| 是否只是换皮 | 否；新增了原项目没有的跨运行比较能力 |
| 是否能展示 | 能；可运行离线 Demo 并打开变化报告 |
| 是否适合写入简历 | 能；必须表述为开源项目二次开发 |

## 已知边界

未测试需要登录、验证码或 JavaScript 渲染的真实商城；未实现 Playwright、通知发送、生产部署和大规模性能测试。“商品消失”只表示最新一次成功快照中没有观察到，不等同于商品已下架。
